import { useState, useEffect } from 'react';
import classNames from 'classnames/bind';
import style from './SerachInput.module.scss'

const cx = classNames.bind(style);

interface Props {
	
}

const SerachInput = ({}: Props) => {
	return (
		
	);
}

export { SerachInput };