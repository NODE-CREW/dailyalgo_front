import type { Meta, StoryObj } from '@storybook/react';
import { SerachInput } from './SerachInput';

const meta: Meta<typeof SerachInput> = {
	component: SerachInput,
	argTypes: {
		
}
	};

export default meta;
type Story = StoryObj<typeof SerachInput>;

export const Default: Story = {
	args: {
		
	}
};